use LWP::Simple;
use strict;

my %lc;
foreach my $line ("\n", split(get('http://catalog.hathitrust.org/static/callnoletters.txt'))) {
    my ($letters, $count) = split("\t", $line);
    $lc{$letters} = $count;
}
